.git
.github
.cursor
.config
# Excluded because it extends the excluded ./.config/tsconfig.json; Playwright 1.62+ hard-fails on the unresolvable extends. Unused in the image (build:cli uses tsconfig.cli.json).
tsconfig.json
# Repository-only architecture tooling: this image uses COPY . ., so the concern
# registry and CLI would otherwise ship inside the E2E runner.
architecture
.planning
.vscode
node_modules
dist
coverage
test-results
playwright-report
*.log
.env
.env.*
playwright/.auth/
