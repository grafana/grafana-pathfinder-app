# CLAUDE.md

@AGENTS.md — the primary agent reference for this repository.

**Memory (Claude Code only):** long-lived user / project / feedback notes live under `~/.claude/projects/<project-slug>/memory/`, where `<project-slug>` is this repo's absolute path with `/` replaced by `-`. See the `auto memory` section in the system prompt for the file format.
