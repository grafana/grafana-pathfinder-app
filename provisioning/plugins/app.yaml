apiVersion: 1

apps:
  - type: 'grafana-pathfinder-app'
    org_id: 1
    org_name: 'grafanalabs'
    disabled: false
    jsonData:
      # Set to true after successful registration with Coda
      codaRegistered: false
