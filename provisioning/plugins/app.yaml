apiVersion: 1

apps:
  - type: 'grafana-pathfinder-app'
    org_id: 1
    org_name: 'grafanalabs'
    disabled: false

  # Sandbox VMs and terminals. Optional — Pathfinder hides its terminal UI when
  # this plugin is absent.
  - type: 'grafana-coda-app'
    org_id: 1
    org_name: 'grafanalabs'
    disabled: false
